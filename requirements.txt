# Core
anthropic>=0.25.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0

# Scraping
feedparser>=6.0.11
trafilatura>=1.9.0      # actively maintained article text extractor (replaces newspaper3k)
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=5.2.0

# Matching
# rapidfuzz is preferred for token_set_ratio (handles word reordering)
# Falls back to stdlib difflib if rapidfuzz unavailable
rapidfuzz>=3.9.0
pandas>=2.2.0

# Alerts
sendgrid>=6.11.0

# Dashboard API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Infra / utils
boto3>=1.34.0          # AWS S3 archiving
google-cloud-storage>=2.16.0  # GCS archiving (optional)
tenacity>=8.3.0        # retry logic

# Dev / testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # for testing FastAPI
